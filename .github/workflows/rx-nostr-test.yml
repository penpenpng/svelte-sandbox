name: rx-nostr test

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rx-nostr-test
    steps:
      - run: npm install
      - run: node index.js
